# SyftHub MCP Server Configuration
# Copy this file to .env and update values as needed

# =============================================================================
# Server Configuration
# =============================================================================
ENVIRONMENT=development
LOG_LEVEL=debug
MCP_PORT=8002

# =============================================================================
# OAuth 2.1 Configuration (proxy-aware URLs)
# =============================================================================
# These URLs are what external clients see (through nginx proxy)
OAUTH_ISSUER=http://localhost:8080/mcp
OAUTH_AUDIENCE=mcp-server
API_BASE_URL=http://localhost:8080/mcp

# =============================================================================
# SyftHub Integration
# =============================================================================
# Internal URL for MCP server to communicate with SyftHub backend
# In Docker: http://backend:8000
# Standalone: http://localhost:8000
SYFTHUB_URL=http://backend:8000

# Public URL for the SyftHub frontend (used in register link on login page)
# In Docker with proxy: http://localhost:8080
# Production: https://your-domain.com
SYFTHUB_PUBLIC_URL=http://localhost:8080

# =============================================================================
# Production Configuration Example
# =============================================================================
# Uncomment and update for production deployment:

# ENVIRONMENT=production
# LOG_LEVEL=info
# MCP_PORT=8002

# OAuth (use your production domain)
# OAUTH_ISSUER=https://your-domain.com/mcp
# OAUTH_AUDIENCE=mcp-server
# API_BASE_URL=https://your-domain.com/mcp

# SyftHub (internal Docker network)
# SYFTHUB_URL=http://backend:8000
# SYFTHUB_PUBLIC_URL=https://your-domain.com
